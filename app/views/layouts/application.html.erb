<!DOCTYPE html>
<html>
  <head>
    <title>SomesplashTeam1</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header>
      <div class="container">
        <div class="header navbar">
          <%=link_to image_tag("/images/logo.png"), categories_path%>
          <%=form_with url: "/search", method: :get do |f|%>
            <%=f.label :query, "Search" %>
            <%=f.text_field :query %>
          <%end%>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer footer__content">
      <div> Build with ♥ by
      
      </div>
    </footer>
  </body>
</html>
